// These are temporary, insecure rules to allow for initial development.
// They grant read/write access to any authenticated user.
// DEPLOY THE SECURE RULES BEFORE GOING TO PRODUCTION.
// See README.md for instructions.
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
